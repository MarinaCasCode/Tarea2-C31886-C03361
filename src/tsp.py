# Problema d) Vendedor (TSP).
#
# (pendiente) Aquí irá la clase ProblemaVendedor con los 3 métodos:
#   busqueda_greedy, busqueda_exhaustiva_pura, busqueda_exhaustiva_ra
# siguiendo el mismo estilo que src/assignment.py (el código del profe).
#
# Datos: tabla NxN de distancias; un -1 = "no hay camino" (confirmado).
# PENDIENTE preguntar al profe: ¿el recorrido regresa al origen (ciclo) o
# termina sin regresar (camino abierto)?
